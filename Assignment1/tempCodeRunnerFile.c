while (1)
    {
        printf("Enter size of the matrix A (m n): ");
        scanf("%d %d", &m, &n);

        if (m>0 && n>0)
        {
            
            break;
        }
        printf("\nOnly enter positive values!\n");
    }